# Standalone Blender (bpy) scripts. Modules here that import `bpy` run inside
# Blender's bundled Python and must NOT be imported from the scene_physics package.
# Pure-data modules (e.g. material_specs.py) are bpy-free and safe to import.
